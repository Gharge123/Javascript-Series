
//Template literals

//using backticks (` or ") use variable ${..}.

//Syntax `string text ${expression} string text`

//Program -

// const name = "Rutuja";

// const message = `hello , ${name}`;

// console.log(message);

let a = 20;
let b = 23;

function demo() {
    a = 34;
    b = 35;
    console.log(`Multiplication of a and b is, ${a * b}`);
    console.log(`Addition of a and b is, ${a + b}`);
    console.log(`Subtraction of a and b is, ${a - b}`);
    console.log(`Division of a and b is, ${a / b}`);


}
demo();





let a = 10;
let b = 20;

const name = "Gharge"

let result = `addition of a and b is, ${a+b}`;

console.log(`Rutuja ${name}`);
console.log(result);

function demo(){

    a = 40;
    console.log(`both number multiplication is, ${a*b}`);

    console.log(result);
  
}
demo();

